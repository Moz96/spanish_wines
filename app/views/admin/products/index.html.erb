<div class="main-container">
  <div class="product-grid">
    <% @products.each do |product| %>
        <div class="product">
          <div class="image-container">
            <% if product && product.image.present? %>
              <%= image_tag product.image, class: "product-image" %>
            <% else %>
              <%= image_tag 'thumbnail_Villa-Abad-Syrah-Tempranillo 2 Background Removed.png', class: "product-image" %>
            <% end %>
          </div>
          <h2><%= link_to product.name, product_path(product) %></h2>
          <div class="product-info">
            <p> <%= product.quantity%> </p>
          </div>

          <%= link_to 'Edit', edit_product_path(product), class: "cart-button" %>
          <%= button_to 'Delete', admin_product_path(product), method: :delete, data: { confirm: 'Are you sure?' }, class: "cart-button" %>
        </div>
    <% end %>
  </div>
</div>
